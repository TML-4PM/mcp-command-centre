AWSTemplateFormatVersion: '2010-09-09'
Description: >
  RETIRED: EventBridge rules removed. Execution is now bridge-driven only.

Resources: {}

Outputs:
  Status:
    Value: "EventBridge retired for this stack"
