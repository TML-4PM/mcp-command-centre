# T4H Execution & Asset Control Contract v2.1
*Status: **LIVE · 16/16 REAL · 100%** · Verified 2026-04-22*

> Canonical path: `core.contract_sections`  
> Verify: `SELECT * FROM core.v_contract_compliance;`  
> RPC: `ops.rpc_contract_verify()` · Cron job `251` @ `:17` hourly  
> Entity: `t4h.contract.v2_1` · Broadcasts: `225`, `227`

---

## 0. Core principle

The system does not describe reality. It enforces reality.

An asset, workflow, or output is **REAL** iff:

1. Registered in `core.registry_entities`
2. Executed via bridge or `pg_cron`
3. Produces a verifiable artefact (row, file, log, broadcast)

Everything else is **PARTIAL** or **PRETEND**. PRETEND is a reject state.

---

## 1. Sixteen sections — all BACKED, all REAL

| ID | Section | Backing | Status |
|---|---|---|---|
| S0 | Core principle | policy | REAL |
| S1 | Nothing exists unless registered | `core.registry_entities` (547) | REAL |
| S2 | First-30s value contract | policy | REAL |
| S3 | INPUT→CLASSIFY→ROUTE→EXECUTE→STORE→FEEDBACK→LOOP | `cron.job` (166) + registry | REAL |
| S4 | Artefact + evidence + storage (Wave10) | `t4h_canonical_changes` (72/7d) | REAL |
| S5 | Execute-first; clarify only on BLOCKED | policy | REAL |
| S6 | REAL/PARTIAL/PRETEND labels | `contract_sections.status` | REAL |
| S7 | Minimum viable system | registry + cron | REAL |
| **S8** | **Prove invisible assets exist** | **`core.v_asset_drift` (429 rows)** | **REAL** |
| S9 | Auto drift + no-human execution | `cron.job` (166) | REAL |
| S10 | Idempotent writes (BP 12) | `public.email_idempotency` | REAL |
| **S11** | **Rollback-first (BP 11)** | **`core.rollback_snapshots` + FK to registry** | **REAL** |
| S12 | Kill switch per build (BP 13) | `public.cap_secrets` | REAL |
| **S13** | **RDTI tag at creation (BP 16)** | **`ops.trg_require_rdti_tag` BEFORE INSERT** | **REAL** |
| S14 | Canonical change broadcast | `t4h_canonical_changes` | REAL |
| S15 | AUTONOMOUS/LOG-ONLY/GATED/BLOCKED | `registry_entities.autonomy_tier` | REAL |

### Enforcement rule

If an asset is not in `core.registry_entities` within 24 hours → **treated as non-existent**.

---

## 2. Proof of invisible assets (S8)

`core.v_asset_drift` surfaces assets that exist in source-of-truth tables but not in the canonical registry:

| asset_class | drift_state | count |
|---|---|---|
| lambda | MISSING | 382 |
| lambda | DUPLICATE | 4 |
| domain | MISSING | 43 |

These are the assets you cannot see manually. The detector runs on every compliance verify.

---

## 3. Rollback (S11)

```
core.rollback_snapshots
  entity_key FK → core.registry_entities(entity_key)
  snapshot_name, storage_uri, rollback_sql, valid_until
  UNIQUE (entity_key, snapshot_name)
```

Contract v2.1 registered its own rollback:
- snapshot: `v2_1_deploy_2026_04_22`
- storage: `s3://arch-maturity/rollback/contract_v2_1_pre_deploy`
- embedded SQL restores pre-deploy state

---

## 4. RDTI enforcement (S13)

```
ops.trg_require_rdti_tag (BEFORE INSERT on core.registry_entities)
  applies to: entity_type ∈ {system, agent, workflow, job}
              AND autonomy_tier ∈ {AUTONOMOUS, GATED}
  requires:   metadata ? 'is_rd' AND metadata ? 'project_code'
  bypass:     tag 'no-rdti'
```

Tested: rejects un-tagged (`sql_error`), accepts bypass.

---

## 5. Autonomy tiers

| Tier | Scope | HITL |
|---|---|---|
| AUTONOMOUS | SELECTs, views, RPCs, indexes, reads | No |
| LOG-ONLY | INSERTs, UPDATEs, email | Log line |
| GATED | DELETE, DROP, RLS, deploy, IAM | Dry-run → exec |
| BLOCKED | Payments, DNS, creds, legal | Explicit authority |

---

## 6. Wave10 (8 components)

`runtime` · `value-loop` · `revenue` · `distribution` · `observability` · `recovery` · `evidence` · `lifecycle`

Any missing ⇒ PARTIAL.

---

## 7. Verification

```sql
SELECT * FROM core.v_contract_compliance ORDER BY section_id;
SELECT * FROM ops.rpc_contract_verify();
SELECT asset_class, drift_state, count(*) FROM core.v_asset_drift GROUP BY 1,2;
SELECT * FROM core.rollback_snapshots WHERE entity_key='t4h.contract.v2_1';
```

Hourly auto-verify: `cron.job` id `251`, schedule `17 * * * *`.  
Verification log: `core.contract_verifications` (64 rows after 4 runs).

---

## 8. Rollback

```sql
-- See core.rollback_snapshots WHERE entity_key='t4h.contract.v2_1'
```

---

## 9. Ownership

| Layer | Owner |
|---|---|
| Registry | `core.registry_entities` |
| Detection | `verify_sql` per section + `v_asset_drift` |
| Execution | Bridge + Lambda + `pg_cron` |
| Expansion | Claude / Gemini agents |
| Enforcement | Troy (GATED + BLOCKED policy gate) |

---

*Generated by Claude via bridge `zdgnab3py0` · 2026-04-22 · GitHub `TML-4PM/mcp-command-centre/claude-outputs/contract/v2_1/` · Drive `1G7Puf0lbkPyRoTi1js8wIAxR499WP5wU`.*
